# API Configuration
API_KEY=sk-embedding-api-secret-key-20260114
PORT=6008

# Model Configuration
MODEL_PATH=./models/Qwen3-Embedding-4B
DEVICE=cpu  # cuda or 'cpu' for CPU-only
EMBEDDING_DIMENSION=2560

# GPU Configuration (if using GPU)
#CUDA_VISIBLE_DEVICES=0  # Specify GPU ID(s), e.g., 0,1,2,3

# Logging
LOG_LEVEL=INFO  # DEBUG, INFO, WARNING, ERROR, CRITICAL

# Concurrency Configuration
WORKERS=1  # 并发进程数，暂时设置为1以避免内存不足问题
# 大模型需要大量内存，每个进程都会加载完整模型，所以需要根据系统内存调整