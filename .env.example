# API Configuration
API_KEY=your-secret-key-here
PORT=6008

# Model Configuration
MODEL_PATH=/app/models/Qwen3-Embedding-4B
DEVICE=cuda  # or 'cpu' for CPU-only
EMBEDDING_DIMENSION=2560

# GPU Configuration (if using GPU)
CUDA_VISIBLE_DEVICES=0  # Specify GPU ID(s), e.g., 0,1,2,3

# Logging
LOG_LEVEL=INFO  # DEBUG, INFO, WARNING, ERROR, CRITICAL
